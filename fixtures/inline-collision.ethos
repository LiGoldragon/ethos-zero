; The audit's row-15 probe, grown: P and Q each declare a payload X in
; place, so each payload is named for its enum and the authored X_Data
; stays free; a payload unique in the file keeps its short name.
Library
[]
[ P.[ X.{ String }  Y.[ X.{ Integer } ] ]
  Q.[ X.{ Integer } ]
  X_Data.String
  R.[ Z.{ String } ] ]
[]
[]
